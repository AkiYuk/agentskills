# CLAUDE.md

## プロジェクト概要

- **リポジトリ**: agentskills
- **ライセンス**: MIT

## 言語・コミュニケーション

- コードコメントは日本語で記述する
- コミットメッセージは日本語で記述する
- PRのタイトル・説明は日本語で記述する
- ユーザーへの応答は日本語で行う

## コーディング規約

- シンプルさを優先し、過度な抽象化を避ける
- 既存のコードスタイル・パターンに従う
- 変更していないコードに対してリファクタリングやコメント追加を行わない
- セキュリティ上の脆弱性（インジェクション、XSS等）を持ち込まない

## チケット駆動開発

### 基本原則

- 全ての作業はチケットを起点に行う
- **チケットで要件定義と設計を固めてから実装に入る**
- 人間がレビュー・承認するまで実装を開始しない

### チケット管理

- チケットはステータス別ディレクトリにMarkdownファイルで管理する
- ファイル名: `TICKET-XXX-タイトル.md`（XXXは3桁のゼロ埋め連番）
- テンプレート: `tickets/_template.md` を使用する
- 番号の採番: `tickets/` 配下の全ディレクトリから既存チケットの最大番号 + 1
- ステータス変更時はチケットファイルを該当ディレクトリに移動する

### ディレクトリ構成

```
tickets/
  _template.md
  todo/          # 要件定義・設計が記載済み。レビュー待ち
  in-progress/   # 人間が承認済み。実装中
  review/        # 実装完了。コードレビュー待ち
  done/          # 完了。PRマージ可能
```

### ワークフロー

```
1. チケット作成（要件定義・設計を記載）
   → tickets/todo/ に配置
        ↓
2. 人間がチケットをレビュー・承認
   → tickets/in-progress/ に移動
        ↓
3. 作業ブランチを作成し実装
   ブランチ名: feature/TICKET-XXX-概要
        ↓
4. 実装完了、PR作成
   → tickets/review/ に移動
        ↓
5. コードレビュー承認
   → tickets/done/ に移動
        ↓
6. PRマージ（チケットがdoneであること）
```

### チケット作成ルール

- タイトルは短く、チケットの内容が一目でわかるようにする
- ヘッダーは `# TICKET-XXX: タイトル` の形式にする
- 概要・背景・目的を明確に記述する
- 要件定義を箇条書きで漏れなく記載する
- 設計セクションに変更対象ファイルと設計方針を記載する
- 受け入れ条件を定義する
- **実装の指示があるまでコードを書かない**

### ステータス遷移の責務

| 遷移 | 実行者 | 条件 |
|------|--------|------|
| → `todo` | Claude | チケット作成時。要件定義・設計を記載 |
| `todo` → `in-progress` | 人間 | チケットのレビュー・承認後 |
| `in-progress` → `review` | Claude | 実装完了・PR作成後 |
| `review` → `done` | 人間 | コードレビュー承認後 |

## Git運用ルール

### ブランチ戦略

- `main`: 安定版。直接コミットしない
- `feature/TICKET-XXX-*`: チケットに紐づく新機能の開発
- `fix/TICKET-XXX-*`: チケットに紐づくバグ修正
- `docs/TICKET-XXX-*`: チケットに紐づくドキュメントの追加・修正
- `chore/*`: チケット不要の軽微な作業（CLAUDE.md更新等）

### コミットメッセージ

以下の形式に従う:

```
<種別>(TICKET-XXX): <変更内容の要約>

<必要に応じて詳細説明>
```

種別:
- `feat`: 新機能
- `fix`: バグ修正
- `docs`: ドキュメント
- `refactor`: リファクタリング
- `test`: テストの追加・修正
- `chore`: その他の雑務

### PRルール

- PRはチケット単位で作成する
- PR説明にチケット番号を明記する
- 変更内容をPR説明に明記する
- **チケットが `done/` に移動するまでPRをマージしない**

## 開発フロー（まとめ）

1. チケットを作成し、要件定義・設計を記載する（→ `todo/`）
2. 人間がチケットをレビューし承認する（→ `in-progress/`）
3. 承認後、`main` から作業ブランチを切り実装する
4. PR を作成する（→ `review/`）
5. コードレビュー承認後、チケットを完了にする（→ `done/`）
6. チケットが `done/` にあることを確認し、PR をマージする
